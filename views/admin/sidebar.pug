.sidebar

  .sidebar-top

    a.logo(href="/")

      img(src="/images/fotaza-logo-1.png" width="50px")

  .sidebar-menu

    a.sidebar-link(href="/admin/home" class=isActive("/admin/home") ? "active" : "")
      i.bi.bi-house

    a.sidebar-link(href="/admin/validadores" class=isActive("/admin/validadores") ? "active" : "")
      i.bi.bi-people

  .sidebar-bottom

    a.sidebar-link(href="/auth/logout")
      i.bi.bi-box-arrow-left
